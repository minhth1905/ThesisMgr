<h2><% provide(:title, "Quản lý hồ sơ") %></h2>
<ul class="nav nav-tabs" role="tablist">
  <li class="active"><a href="#home" role="tab" data-toggle="tab">NHẮC NHỞ VIỆC NỘP HỒ SƠ</a></li>
  <li><a href="#profile" role="tab" data-toggle="tab">KIỂM TRA HỢP THỨC HỒ SƠ</a></li>
  <!-- <li><a href="#search" role="tab" data-toggle="tab">TÌM KIẾM VÀ NHẬP</a></li> -->
  <li><a href="#settings" role="tab" data-toggle="tab">DOCUMENT</a></li>
</ul>
<div class="tab-content">
    <div class="tab-pane active" id="home">
        <% if @check != 0 %>
          <% if @topic.blank? %>
            <p>Không có sinh viên nào để gửi thông báo</p>
          <% else %>
            <%= render "notifi" %>
          <% end %>
        <% else %>
          <p>Chưa đủ điều kiện để gửi email nhắc nhở</p>
        <% end %>
    </div>
    <div class="tab-pane" id="profile">
        <%= render "check_file" %>
    </div>
    <div class="tab-pane" id="search">
        <%#= render "search" %>
    </div>
    <div class="tab-pane" id="settings">
        DOCUMENT
    </div>
</div>
