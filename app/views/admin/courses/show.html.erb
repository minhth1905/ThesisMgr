<table class="table">
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>
  <tbody>
      <tr>
        <td><%= @course.name %></td>
        <td><%= link_to "edit", edit_admin_course_path(id: @course.id) %></td>
        <td><%= link_to "delete", [:admin, @course], method: :delete,
          data: {confirm: t("comfirm")} %></td>
      </tr>
  </tbody>
</table>
