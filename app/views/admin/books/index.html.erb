<h2><% provide(:title, "Quản lý nộp quyển") %></h2>
<ul class="nav nav-tabs" role="tablist">
  <li class="active"><a href="#home" role="tab" data-toggle="tab">GỬI EMAIL NHẮC NHỞ</a></li>
  <li><a href="#profile" role="tab" data-toggle="tab">NỘP QUYỂN</a></li>
  <li><a href="#search" role="tab" data-toggle="tab">TÌM KIẾM VÀ NHẬP</a></li>
  <li><a href="#settings" role="tab" data-toggle="tab">DOCUMENT</a></li>
</ul>
<div class="tab-content">
    <div class="tab-pane active" id="home">
        <% if @check == 1 %>
          <%= render "notifi" %>
        <% else %>
          <p>Chưa đủ điều kiện để thông báo bảo vệ</p>
        <% end %>
    </div>
    <div class="tab-pane" id="profile">
        <%= render "receive_file" %>
    </div>
    <div class="tab-pane" id="search">
        <%= render "search" %>
    </div>
    <div class="tab-pane" id="settings">
        DOCUMENT
    </div>
</div>
